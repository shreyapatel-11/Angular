import { ColumnChartDirective } from './column-chart.directive';

describe('ColumnChartDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnChartDirective();
    expect(directive).toBeTruthy();
  });
});
